server:
  port: 9098
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/product-services
    driverClassName: org.postgresql.Driver
    username: postgres
    password: admin

  jpa:
    showSql: true
    properties:
      hibernate:
        formatSql: true
        ddl-auto: update
    databasePlatform: org.hibernate.dialect.PostgreSQLDialect

  application:
    name : product-service









